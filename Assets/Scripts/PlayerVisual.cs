using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerVisual : MonoBehaviour
{
    // [SerializeField] private float rotationSpeed = 100f;


    // private void Update() {
    //     float rotateInputY = Input.GetAxis("Vertical");
    //     float rotateInputX = Input.GetAxis("Horizontal");

    //     transform.Rotate(0f, 0f, -1 * rotateInputX * rotationSpeed * Time.deltaTime);

    //     Debug.Log(rotateInputX);

    //     if (rotateInputX == 0){
    //         transform.Rotate(0f, 0f, -2 * transform.rotation.z * rotationSpeed * Time.deltaTime);
    //     }
    // }
}
